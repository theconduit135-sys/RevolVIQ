import { UnderwritingScanner } from "@/components/features/UnderwritingScanner";

export default function ScanPage() {
    return (
        <div className="min-h-screen bg-midnight-900 flex flex-col items-center justify-center p-6">
            <UnderwritingScanner />
        </div>
    );
}
